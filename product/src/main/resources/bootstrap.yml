server:
  port: 82
spring:
  application:
    name: product
  cloud:
    nacos:
      config:
        server-addr: 123.249.79.20:8848
        prefix: swh
        file-extension: yml
  profiles:
    active: datasource,upload,register,redis