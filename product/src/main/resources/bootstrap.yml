server:
  port: 82
spring:
  application:
    name: product
  cloud:
    nacos:
      config:
        server-addr: 192.168.80.121:8848
        prefix: swh
        file-extension: yml
  profiles:
    active: datasource,upload,register,redis