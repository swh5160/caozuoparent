server:
  port: 85
spring:
  application:
    name: email
  profiles:
    active: register,rabbitmq,email
  cloud:
    nacos:
      config:
        server-addr: 123.249.79.20:8848
        prefix: swh
        file-extension: yml