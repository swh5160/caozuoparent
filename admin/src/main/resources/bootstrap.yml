server:
  port: 81
spring:
  application:
    name: admin
  profiles:
    active: datasource,upload,register,rabbitmq
  cloud:
    nacos:
      config:
        server-addr: 192.168.80.121:8848
        prefix: swh
        file-extension: yml

